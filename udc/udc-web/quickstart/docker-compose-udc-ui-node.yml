version: '2'
services:
  udc-ui:
    container_name: udc-ui
    build:
        context: ${UDC_HOME}
        dockerfile: ${UDC_HOME}/Dockerfile_node
    ports:
      - 80:80
    env_file:
      - ./set-env.env
    networks:
      - udc-net

networks:
  udc-net:
